# 漫剧排行榜验证（2026-09-22）

## 功能与来源

- 顶部新增「排行榜」，独立展示漫剧热播榜；推荐/搜索选择短剧时，排行榜仍然只读取漫剧。
- 来源为 `https://hongguoduanju.com/rank/hot-comic-drama`。此前漫剧推荐已使用该列表，本次补充独立入口、网站名次、热度、更新时间、总页数和刷新。
- 前三名突出显示，确认卡片进入原有详情与播放流程；返回详情前的排行榜页码和卡片焦点会恢复。刷新重新读取第 1 页。
- 同时支持流式 JSON、旧版序列化 JSON 和仅有服务端渲染卡片的响应。名次使用来源字段，不按本页下标重新编号；缺失名次/热度显示暂无。
- 排名元数据不写入收藏与观看记录。加载失败保留重试入口；加载期间切换标签不会缓存新页码与旧页内容的组合。

## 自动检查

```sh
./kotlin-tv/gradlew -p kotlin-tv :core:test :app:lintDebug :app:assembleDebug :app:lintRelease :app:assembleRelease
./kotlin-tv/gradlew -p kotlin-tv :core:run --args=--ranking
python3 kotlin-tv/tools/verify-comic-ranking-evidence.py outputs/comic-ranking
```

- 27 项 JVM 测试通过。新增 5 项覆盖来源名次/热度/更新时间、缺失名次、DOM 第 2 页、错页响应和 DOM 末页不含当前页链接。新增解析测试先观察失败，再实现或修正。
- Debug / Release lint 均为 0 错误、11 警告；两种 APK 构建成功。Release 使用本机开发签名验证 R8 混淆构建，不是正式签名发布。
- 真实接口探测：第 1 页 1–20 名、第 2 页 21–40 名、第 5 页 81–100 名，各 20 条；抽查页面之间无重复 ID，热度不为空，末页无后续页。来源更新时间为「9月22日已更新」。榜单内容会随平台更新。
- 独立代码审查发现的末页总数和刷新缓存问题均已修复并复查。

## Android TV 验收

Android TV API 34 ARM64 模拟器，1920×1080，安装最终混淆 Release 构建：

- 从短剧推荐进入排行榜，仍显示漫剧榜；名次、热度、更新时间可见。
- 遥控器连续翻到第 2、3、4、5 页，首项分别为第 21、41、61、81 名；末页可见第 100 名和「第 5 / 5 页」，下一页禁用。
- 从第 23 名进入详情，返回后恢复该卡片焦点和第 2 页。
- 向上能到达「刷新榜单」，刷新后回到第 1 页。
- GSM 限速下，从第 2 页刷新并切换到收藏，再返回排行榜，界面重新加载且没有旧排名；恢复网络后显示第 1 名。
- 1.5 倍系统字号下，顶部导航、刷新按钮、名次和热度可见；长标题按两行省略。
- 收藏、最近观看、设置和搜索仍进入对应页面，内容类型偏好保留。
- 保存的 XML 通过 25 条验收断言；截图人工检查正常字号及放大字号。

本地证据在被 Git 忽略的 `outputs/comic-ranking/`：`final-build.log`、`live-probe.log`、`release-*.xml/png`。验证脚本检查已采集 XML，不会自行驱动设备，也不代表重新运行了模拟器验收。

本地测试包：`outputs/hongguotv-native-comic-ranking-test.apk`，SHA-256：

```text
771b334500eb5a3cc50c915c1d5c77c1dcbeaf5b63f0c72d5129da06e8927343
```

## 交付边界

本次交付为源码提交与推送，未新建 GitHub Release。已发布的 0.2.1 APK 不含新增功能；测试包采用开发签名，不能覆盖不同签名的正式版。此次验证覆盖排行榜及详情往返；音频输出、实体电视遥控器和长期稳定性仍以实机验证为准。
